# ===================================================
# Aurelia Studio v2 - Environment Configuration
# ===================================================
#
# This file serves as a template for environment variables.
# When deploying with Coolify, add these variables through
# the Coolify dashboard instead of using a .env file.
#
# ===================================================

# ===================================================
# REQUIRED API KEYS
# ===================================================

# Replicate API Token (REQUIRED for AI Image Generation)
# Get your token from: https://replicate.com/account/api-tokens
# Used by the "Generate" tab for AI-powered artwork creation
REPLICATE_API_TOKEN=

# ===================================================
# OPTIONAL API KEYS
# ===================================================

# Anthropic API Key (OPTIONAL for AI-Powered Listing Generation)
# Get your key from: https://console.anthropic.com/
# Used by the "Listing" tab for automatic product description generation
# If not provided, the app will use template-based listing generation
ANTHROPIC_API_KEY=

# ===================================================
# SERVER CONFIGURATION
# ===================================================

# Server Port
# Default: 3000
# Coolify will typically handle port mapping automatically
PORT=3000

# Node Environment
# Options: production, development
# Default: production
NODE_ENV=production

# ===================================================
# DATA STORAGE
# ===================================================

# Data Directory Path
# This is where uploaded files, mockups, and processed images are stored
# Default: /data
# For Coolify: Use /data or configure a persistent volume
DATA_DIR=/data

# ===================================================
# DEPLOYMENT NOTES FOR COOLIFY
# ===================================================
#
# 1. Add environment variables in Coolify dashboard under:
#    Application > Environment Variables
#
# 2. Required variables:
#    - REPLICATE_API_TOKEN (for AI generation to work)
#
# 3. Optional variables:
#    - ANTHROPIC_API_KEY (enhances listing generation)
#    - PORT (if you need a custom port)
#    - DATA_DIR (if you want custom storage location)
#
# 4. Persistent Storage:
#    - Configure a persistent volume mounted to /data
#    - This ensures mockups and uploads survive container restarts
#
# 5. Health Check:
#    - Path: /api/health
#    - Port: 3000 (or your custom PORT)
#
# ===================================================
